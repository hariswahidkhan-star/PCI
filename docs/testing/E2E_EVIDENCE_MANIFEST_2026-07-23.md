# E2E Evidence Manifest - 2026-07-23

Branch: `cursor/fix-pml-ai-e2e-d975`

This manifest defines the evidence expected from the branch's Playwright and CI runs. It does not
claim those artifacts exist until CI has executed and published them.

## CI run

- CI run URL: **TBD**
- Workflow/job: `.github/workflows/build.yml` -> `e2e`
- CI artifact currently configured by workflow:
  - Artifact name: `playwright-report`
  - Source path: `frontend/playwright-report`
  - Retention: 7 days

## Playwright runtime configuration

Configured in `frontend/playwright.config.ts`:

- Test directory: `frontend/e2e`
- Projects: `chromium`, `firefox`, `webkit`, `mobile-chrome`, `mobile-safari`
- Screenshots: `only-on-failure`
- Video: `retain-on-failure`
- Trace: `on-first-retry`
- Reporter in CI: `github` plus `list`
- Web server: built backend DLL, waiting on `/api/health`

## Expected evidence artifacts

| Artifact type | Expected location | Produced by | Notes |
|---|---|---|---|
| Story screenshots | `frontend/test-results/**/<story-name>.png` | Explicit `storyScreenshot(page, testInfo, name)` calls in e2e specs | Screenshots document successful journey checkpoints when the relevant test executes. |
| Failure screenshots | `frontend/test-results/**/*.png` | Playwright `screenshot: only-on-failure` | Expected only for failed tests. |
| Failure videos | `frontend/test-results/**/*.webm` | Playwright `video: retain-on-failure` | Expected only for failed tests. |
| Retry traces | `frontend/test-results/**/*.zip` | Playwright `trace: on-first-retry` | Expected when a test retries; open with `npx playwright show-trace`. |
| HTML/report bundle | `frontend/playwright-report/**` | Playwright report generation, if present | The current CI upload points here. If no report is generated by the configured reporters, this artifact may be empty/ignored. |
| CI logs | GitHub Actions job log for `e2e` | `npm run e2e` | Primary source for pass/fail until artifacts are attached. |

## Current evidence expectation

- Local discovery only: **91 listed Playwright executions** across **18 spec files** and **5 projects**.
- Runtime evidence is **PENDING-CI**.
- Playwright journeys must not be marked passed until the CI run URL above is populated and the e2e job is green.

## Artifact collection note

The workflow currently uploads `frontend/playwright-report`. Raw Playwright output, including explicit
story screenshots and failure traces/videos, is written under `frontend/test-results` by default. If
the CI report artifact does not include those raw files, attach `frontend/test-results` in a future
workflow update or enable an HTML reporter that embeds/links the attachments.
