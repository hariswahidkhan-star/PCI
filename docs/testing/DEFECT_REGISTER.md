# Defect Register

_A living record of defects and notable behavioural findings surfaced by the PCI testing programme.
Each row states the finding, how it was surfaced, its status, and the evidence. Findings are recorded
honestly — a test that exposes a defect is never weakened or deleted to go green; the defect is fixed
(with a regression test) or logged here with its residual risk._

Status legend: **FIXED** (corrected + regression test) · **FIXED** (corrected on the
current branch; CI evidence pending) · **OPEN** (real, not yet fixed) ·
**DEFERRED** (real, out of the current increment's scope, with rationale) · **BY-DESIGN** (confirmed
intended behaviour after review).

## Register

| ID | Area | Finding | Severity | Surfaced by | Status | Evidence / resolution |
|----|------|---------|----------|-------------|--------|-----------------------|
| DEF-1 | Security — CSV export | Admin analytics + partner-portal CSV exports RFC-4180-quoted delimiters but did **not** neutralise spreadsheet formula triggers (`= + - @`), so an attacker-controlled `utm_*` / `referrer` / `landing` value could execute as a formula when an admin opens the file (CWE-1236 / CSV injection). | High | Phase-2 re-audit (SEC-2) | **FIXED** | Shared `Core/Csv.Field` (formula-injection guard on non-numeric `= + - @` / TAB / CR, then RFC-4180 quote; genuine numbers preserved). All three export sites routed through it (AdminAnalytics + PartnerPortal were vulnerable; AdminMgmt already guarded, now consolidated & no longer corrupts negatives). Regression: `CsvTests` (18). Verified xUnit 425/425 + integration 956/956 on SQLite **and** MySQL (§59n green). Commit `0666d63`. |
| DEF-2 (P0-7) | Exam authorization — retake wait | `ExamAuthorization.ResolveWindow` computed a `RetakeWaitDays` surfaced to students / "waivable" by admins, but **no code path ever wrote `exam_authorizations.retake_wait_until`** — so the retake waiting period was never populated or enforced (a dead-end). | Medium | Increment B (BD-9) → fixed in P0-7 | **FIXED** (CI pending) | `EnsureForPayment` now persists `retake_wait_until = last finalized failed sitting + retake_wait_days` on the NEXT (retake) seat (first seats and held/invalidated attempts start no wait), via `ExamAuthorization.ResolveRetakeWaitUntil`; `/api/me/exam/book` enforces it (`retake_wait_active`); the admin `ex_waive_wait` endpoint clears it. Regression: `ExamAuthorizationTests` retake cases (xUnit **464/464**) + `backend/tests/retake_wait_test.py` end-to-end block→waive→book (4/4). No regressions: integration **976/976** on SQLite. |
| DEF-3 | Exam authorization — BD-4 | Membership renewal/dues webhook date-math (renew from `max(expiry, now)`, recert cycle extension, `invoice.paid` / `subscription.*` mirrors) is not independently unit-testable — it is bound to live Stripe objects. | — (coverage gap, not a defect) | Phase-2 re-audit (BD-4) | **DEFERRED** | Recorded in the coverage matrix as integration-layer; exercised through the signed-webhook path in the integration suite, not the unit tier. No behavioural defect observed. |
| DEF-4 | Scoring — BD-10 | `ScoreAttempt` (pass-boundary / rounding) is private behind `InitScorer`, so the exact `pct == passMark` boundary and one-decimal rounding cannot be unit-pinned without a source seam. | — (coverage gap, not a defect) | Phase-2 re-audit (BD-10) | **DEFERRED** | Boundary is covered at the integration layer (§2 pass, §9e12 66.7% vs 80% fail, §3 fail). Adding a minimal test seam is a candidate follow-up. |
| DEF-5 | Certification naming — PML-AI | Interim/legacy PDL-AI / CPMD naming could remain visible or routable instead of the canonical third certification, **PML-AI**. | High | Branch PML-AI audit + E2E discovery | **FIXED** | `backend/Data/MultiCert.cs`, `backend/Data/SeedContent.cs`, public content, BoK assets, and browser specs now align on PML-AI as the third certification with legacy names migrated/redirected. Verified green on CI run https://github.com/hariswahidkhan-star/PCI/actions/runs/30002525905. |
| DEF-6 | Admin security — 2FA GET/guard | Admin 2FA status/setup surfaces were not safely reachable through the expected guarded GET path, risking a broken settings card or an unguarded security action. | High | Branch admin MFA/RBAC journey | **FIXED** | Admin settings/security routes and `admin-security-rbac.spec.ts` cover owner 2FA status visibility and least-privilege denial. Verified green on CI run 30002525905. |
| DEF-7 | Billing/finance — certification-scoped code validation | Certification-scoped discounts could be previewed or accepted against the wrong certification, especially with the new PML-AI third-cert path. | High | Branch billing/founding journey | **FIXED** | Billing validation is cert-aware; `portal-billing-founding.spec.ts` asserts a PML-AI-scoped exam discount rejects PCL-AI and previews for PML-AI. Live Stripe execution remains external; verified on CI run 30002525905. |
| DEF-8 | Honorary/admin setup — setup_url fallback | Admin-created member/honorary setup flows could fail to return a usable `setup_url`, breaking the reset-password handoff for invited users. | Medium | Branch honorary/setup review | **FIXED** | Setup/resend flows preserve a `reset-password.html?token=...` URL fallback. Backend and verified on CI run 30002525905. |
| DEF-9 | Audit attribution — proxied actor | Audit entries for impersonated/support-view/proxied admin actions could attribute the event to the wrong actor or lose the effective operator context. | High | Branch audit-attribution review | **FIXED** | Branch changes attribute proxied actions to the effective admin/support actor; `admin-operations.spec.ts` covers the read-only impersonation banner and exit path. See DEF-14 for the remaining schema limitation. Verified green on CI run https://github.com/hariswahidkhan-star/PCI/actions/runs/30002525905. |
| DEF-10 | Account recovery — reset-password handoff | Invalid or expired reset-password links could strand a user instead of handing off cleanly to the portal login. | Medium | Branch student account/security journey | **FIXED** | `portal-account-security.spec.ts` covers invalid reset-token handoff to the portal login, plus non-enumerating forgot-password copy. Verified on CI run 30002525905. |
| DEF-11 | Security — CSV whitespace formula neutralization | CSV formula neutralization did not consistently cover formula triggers preceded by whitespace/control characters, leaving spreadsheet execution risk after export. | High | Branch CSV hardening | **FIXED** | `Core/Csv.Field` neutralizes formula triggers after whitespace/control-prefix handling while preserving genuine non-formula values; `CsvTests` cover the regression. Verified by backend-unit + backend + backend-mysql on CI run 30002525905. |
| DEF-12 | CPD admin review UI | CPD submissions could be made by students without a reliable admin review/approval UI path to update the student's approved total. | Medium | Branch credentials/CPD/documents journey | **FIXED** | `frontend/src/admin/pages/CpdReview.tsx` and `portal-documents-cpd.spec.ts` cover student submission, admin approval, and updated approved total. Verified on CI run 30002525905. |
| DEF-13 | External providers — live execution boundary | Live Stripe, Certuvo, exam-vendor/proctoring, translation-provider, and IDV provider paths are not proven by this branch's local discovery or CI placeholders. | High | Release evidence review | **OPEN** (external) | Tracked as **NOT-CLAIMED (external)** in `E2E_USER_JOURNEY_REPORT_2026-07-23.md`; requires provider sandbox/staging/operator execution and evidence links before a production readiness claim. |
| DEF-14 | Audit logs — `audit_logs.user_id` schema limitation | Some audit attribution improvements can record effective actor context, but the existing `audit_logs.user_id` shape still limits full representation of both the subject user and acting/proxied admin in one normalized record. | Medium | Branch audit-attribution review | **OPEN** | Known schema limitation remains intentionally open; fixing it requires a schema/API contract change rather than only UI or route attribution changes. |
| DEF-17 (P0-6) | Migrations — unversioned + unlocked schema boot | The boot migration ran an idempotent "convergence" (`CREATE … IF NOT EXISTS` / `ADD COLUMN`) with **no version ledger and no lock**: several instances booting at once (rolling deploy / scale-out) could race the DDL and duplicate-key each other, and there was no record of the applied schema, no drift detection, and no guard against an OLDER binary starting on a NEWER schema. | Critical | Deep E2E fix pass (P0-6) | **FIXED** | New `schema_migrations` ledger (`version`, `checksum`, `applied_at`); `Migrate.Run` now runs under a cross-instance lock (`Db.WithMigrationLock` — MySQL `GET_LOCK`, SQLite lock-file), records the applied `SchemaVersion` + schema-file SHA-256 (drift-detected), and **refuses to start** an older binary against a newer schema; `Program.cs` exits (75/70) on incompatibility/failure so the service is never ready on a half-migrated DB. Regression: `backend/tests/migration_versioning_test.py` (8/8 — ledger, idempotent reboot, compatibility refusal, concurrent boot). No regressions: integration **976/976**, xUnit **464/464**. |
| DEF-19 | Payments — exam-fee membership gate not expiry-aware | The standalone-exam checkout gate (`Endpoints/Payments.cs`) accepted any membership with `status='active'`, so a **lapsed** membership (expiry_date in the past) could still buy a standalone exam during the window between expiry and the daily expiry sweep flipping the status to `expired`. | Medium | Payment-mechanism audit (product request) | **FIXED** | The gate is now expiry-aware: `status='active' AND (expiry_date IS NULL OR expiry_date>now)`. A lapsed member must renew (or use the membership+exam bundle) first. Behaviour change was previously pinned by integration §35c ("gate still passes while status='active'"); that assertion is re-pinned to the new policy (blocks a past-expiry membership pre-sweep) plus a positive control (§35c2 future expiry passes). The read model (`/api/me` membership_status, §35b) stays status-driven. Verified: integration **977/977** on SQLite. |
| DEF-15 (P0-1) | Security — impersonation read-only not centrally enforced | An impersonation ("view as student") token was resolved to a normal `UserCtx` (`Core/Auth.cs`) and read-only enforcement was left to each endpoint. Only 3 routes checked `u.Impersonated` (`Casework`, `Documents`, `StudentExam` submit); **every other state-changing student route** (2FA/session, erasure, reviews, directory consent, appeals/accommodations, CPD, events, tickets, membership Stripe portal/subscribe, preferences, messages, exam book/start/…) could be **mutated** by a support session. | Critical | Deep E2E fix pass (P0-1) | **FIXED** (CI pending) | Central fail-closed middleware in `Program.cs`: any non-GET/HEAD/OPTIONS request carrying an impersonation token is refused with a stable `403 impersonation_readonly` before endpoint dispatch, and the blocked attempt is written to the impersonation ledger. New resolver `Auth.ImpersonationToken`. Per-endpoint checks retained as defence in depth. Regression: `backend/tests/impersonation_readonly_test.py` (54/54) — 43-route block table, read-access preserved, unknown-route fail-closed, no DB side effects (2FA secret/erasure), ledger audit, and normal-token unaffected. No regressions: adversarial integration suite **976/976** on SQLite. |
| EXT-P0-01 | Stripe ops docs | `docs/OPERATIONS.md` pointed operators at `POST /api/payments/webhook`, which does not exist (real route is `/api/webhook`). | Critical | External integrations audit 2026-07-23 | **FIXED** | Docs corrected; production preflight + `/api/admin/system-check` verify optional `STRIPE_WEBHOOK_URL` ends with `/api/webhook`. |
| EXT-P0-02 | Stripe settlement | Unpaid `checkout.session.completed`, replayed `invoice.paid`, partial-refund bookkeeping, multi-payment membership revoke, and missing Checkout idempotency keys. | Critical | External integrations audit 2026-07-23 | **FIXED** | Paid-state gate + `async_payment_succeeded`; invoice.paid claimed in `webhook_events`; partial refund amounts recorded; full refund keeps membership when another paid supporting payment exists; Stripe `IdempotencyKey` on session create. |
| EXT-P0-03 | External exam routing | Missing/disabled/unmapped external vendor silently fell back to in-house delivery. | Critical | External integrations audit 2026-07-23 | **FIXED** | `ExamDelivery.ExternalBlockReason` fail-closed; booking refuses and alerts; no local sitting. Regression: `ExtIntegrationsAuditTests`. |
| EXT-P0-04 | Exam vendor jobs | Inline candidate→authorize→schedule could orphan/duplicate on timeout. | Critical | External integrations audit 2026-07-23 | **FIXED** | Orders stay `pending`; `ExamDeliveryDispatcher` leases + retries durable provision. |
| EXT-P0-05 | Multi-instance workers | Dispatchers SELECT then UPDATE without atomic claim. | Critical | External integrations audit 2026-07-23 | **FIXED** | `WorkerLease` + lease columns on outbox/delivery/job tables; applied to comms, integrations, marketing, social, syndication, Certuvo, exam delivery. |
| EXT-P0-06 | Critical email | Welcome/setup sent synchronously via Mailer after settlement — provider outage lost credentials. | Critical | External integrations audit 2026-07-23 | **FIXED** | `Mailer.EnqueueWelcome` durable `comm_outbox` enqueue inside settlement txn; SMTP timeout; regression in `ExtIntegrationsAuditTests`. |
| EXT-P1-01 | Certuvo SSRF | Unrestricted `HttpClient` + unsanitised `api_base`/`login_url`. | High | External integrations audit 2026-07-23 | **FIXED** | `Egress.CreateClient`; save-time + request-time URL guards; HTTPS required in production. |
| EXT-P1-02 | Meta Lead webhook | Accepted unsigned posts when `META_APP_SECRET` unset. | High | External integrations audit 2026-07-23 | **FIXED** | Fail-closed 404 without secret (same as Certuvo); HMAC required when configured; leadgen event ledger dedupe. |
| EXT-P1-03 | Plaintext secrets | Exam/integration/Certuvo/translation secrets stored plaintext. | High | External integrations audit 2026-07-23 | **FIXED** | Envelope-encrypt on write; decrypt on use; legacy plaintext tolerated. |
| EXT-P1-04 | Exam callback query secrets | Accepted `?token=` callback auth. | High | External integrations audit 2026-07-23 | **FIXED** | Header-only `X-PCI-Callback-Token`; replay ledger. |
| EXT-P1-05 | QuickBooks durability | Emit fan-out could fail after event loss risk; refresh tokens not persisted. | High | External integrations audit 2026-07-23 | **FIXED** | Event row kept even if fan-out fails; rotated QBO tokens persisted encrypted. |
| EXT-P1-09 | S3 silent local fallback | Incomplete S3 config fell back to local disk. | High | External integrations audit 2026-07-23 | **FIXED** | Production hard-fail; `Put` refuses misconfigured S3; `S3Misconfigured` surfaced in system-check. |
| EXT-P1-11 | Integration health | No operator view of queues/drift/credentials. | High | External integrations audit 2026-07-23 | **FIXED** | `GET /api/admin/integrations/health` (owner/integrations). |
| DEF-AUDIT-01 | Security — legacy student login bypass | `student-login.html` Sign-in navigated to `student-dashboard.html` without calling `/api/login`, exposing a static “Membership active” dashboard. | Critical | Platform audit Phase 0 2026-07-25 | **FIXED** | Both pages replaced with redirect stubs to `/app/login` and `/app/`. Regression: `frontend/e2e/public-site.spec.ts` legacy hand-off test. See `docs/audit/PHASE_0_PLATFORM_AUDIT_2026-07-25.md`. |
| DEF-AUDIT-02 | Data — production SQLite open before config fail | Production `ConfigIssues` MySQL hard-fail ran **after** `new Db()` + migrations/seeds, so a misconfigured prod host could still open SQLite first. | Critical | Platform audit Phase 0 2026-07-25 | **FIXED** | Pre-open fail-closed block before DB work; explicit World bridge is restricted to `/data/`, and the dedicated production image now requires MySQL. |
| DEF-AUDIT-03 | Data — `/data` invents SQLite path under MySQL | Boot auto-set `DATABASE_FILE=/data/pci.db` whenever `/data` was writable, even if `DB_PROVIDER=mysql`. | High | Platform audit Phase 0 2026-07-25 | **FIXED** | Auto-adopt SQLite path only when provider is not mysql/mariadb. |
| DEF-AUDIT-04 | Testing — browser E2E SQLite-only | Playwright webServer always used `e2e_ci.db` (SQLite), so UI→DB journeys were not proven on MySQL. | High | Platform audit Phase 0 2026-07-25 | **FIXED** (gating path) | `E2E_DB_PROVIDER=mysql` support in `playwright.config.ts` + gating CI job `e2e-mysql`. Default local/SQLite `e2e` job retained for speed. |
| DEF-AUDIT-05 | Architecture — PCI World not React | `/world` and `/world-admin` are server-rendered HTML (`WorldPages` / `WorldAdmin`), not React/TypeScript SPAs. | High | Platform audit Phase 0 2026-07-25 | **OPEN** | Product functional; React migration tracked for Phase 6. |
| DEF-AUDIT-06 | Architecture — Partner portal not React | Partner UI is `wwwroot/partner.html` vanilla JS; finance ledger is .NET. | High | Platform audit Phase 0 2026-07-25 | **OPEN** | React partner portal tracked for Phase 4. |
| DEF-AUDIT-07 | Data — money columns DOUBLE after regenerate | `tools/sqlite_to_mysql.py` mapped `REAL→DOUBLE`; money types in `schema.mysql.sql` relied on fragile hand-tunes that regenerating wiped. Runtime Migrate also created money cols as `REAL` (→ DOUBLE on MySQL). | High | Platform audit Phase 1 2026-07-25 | **FIXED** | Generator emits `DECIMAL(12,2)` for money columns; `Migrate.EnsureMoneyDecimals` upgrades legacy DOUBLE in place; migration integrity §5e–5g gating. See `docs/audit/PHASE_1_MYSQL_MONEY_PARITY_2026-07-25.md`. |
| DEF-AUDIT-08 | Docs — SQLite presented as production default | `MYSQL.md`, `DEPLOY.md`, `Dockerfile`, `.env.example` still described SQLite-on-disk as the recommended/implicit production path despite `render.yaml` MySQL. | Medium | Platform audit Phase 1 2026-07-25 | **FIXED** | Docs and env examples state MySQL required for Production; SQLite scoped to local/CI smoke. |
| DEF-AUDIT-09 | Data — ASP.NET production posture bypass | Pre-open guard read only `ASPNETCORE_ENVIRONMENT`; framework-default Production, `DOTNET_ENVIRONMENT=Production`, and Staging opened/seeded SQLite. | Critical | Platform audit Phase 1 deep re-run 2026-07-25 | **FIXED** | Uses `builder.Environment`; non-local hard blockers run before DB open. `production_config_test.py` covers five fail-closed cases. |
| DEF-AUDIT-10 | Data — PCI World ephemeral production SQLite | Dedicated “production” image created `/data` in the container and booted `pciworld.db` with no variables. | Critical | Platform audit Phase 1 deep re-run 2026-07-25 | **FIXED** | Image pins Production+MySQL and fails before DB open without MYSQL/security configuration. |
| DEF-AUDIT-11 | Data — Oracle MySQL restart DDL | MySQL 8 translation strips `IF NOT EXISTS`; a second full-schema batch could abort on an existing index. MariaDB CI did not execute this path. | Critical | Platform audit Phase 1 deep re-run 2026-07-25 | **FIXED** | Existing named indexes are removed from the MySQL batch before translation; double-boot test + Oracle MySQL 8.4 CI job. |
| DEF-AUDIT-12 | Data — omitted runtime money columns | Salaries and Marketing Centre budgets/CPC/spend/conversion values remained REAL→DOUBLE and the upgrader ran before those tables existed. | High | Platform audit Phase 1 deep re-run 2026-07-25 | **FIXED** | Explicit post-installer manifest enforces DECIMAL(12,2)/DECIMAL(18,6), including presence/precision/scale. |
| DEF-AUDIT-13 | Finance — Stripe metadata overrode settled cents | Checkout could charge one cent less than the payment/commission ledger stored because webhook preferred unrounded metadata to Stripe `amount_total`. | High | Platform audit Phase 1 deep re-run 2026-07-25 | **FIXED** | Checkout metadata is invariant/quantized; integer Stripe `amount_total` is authoritative. Integration regression deliberately supplies disagreeing metadata. |
| DEF-AUDIT-14 | Data migration — false-clean cutover | SQLite→MySQL migration warned/skipped missing tables and silently intersected columns; float reconciliation could still exit 0. | High | Platform audit Phase 1 deep re-run 2026-07-25 | **FIXED** | Missing source tables/columns are discrepancies; explicit money manifest uses `Decimal` quantization and exact totals. |
| DEF-AUDIT-15 | Finance — refunds do not reduce pre-payout commission | Reversal entries do not net against an approved original transaction selected for settlement; stale settlement can overpay after refund. | Critical | Platform audit Phase 1 deep re-run 2026-07-25 | **OPEN** | Requires settlement revalidation/repricing and linked reversal netting; finance remediation increment. |
| DEF-AUDIT-16 | Finance — partial settlement payment replay/concurrency | Partial payment retries can add the same payment again; concurrent reads race; evidence fields are overwritten instead of append-only. | Critical | Platform audit Phase 1 deep re-run 2026-07-25 | **OPEN** | Add append-only settlement-payment rows, idempotency key and locked atomic cumulative update. |
| DEF-AUDIT-17 | Finance — currencies mixed as labels | Agreement/payment currencies can be relabelled and balances/statements offset across currencies without FX transactions. | High | Platform audit Phase 1 deep re-run 2026-07-25 | **OPEN** | Enforce USD-only boundaries or partition every ledger/settlement/report by currency. |
| DEF-AUDIT-18 | Finance — commission mutation failures acknowledged | Stripe payment/refund handlers swallow commission creation/reversal failures and still acknowledge the webhook. | High | Platform audit Phase 1 deep re-run 2026-07-25 | **OPEN** | Transactional outbox or atomic mutation with durable retry/reconciliation required. |
| DEF-AUDIT-19 | Finance — refund reporting disagreement | Status-filtered reports, analytics and partner campaign totals disagree on gross/refund/net revenue. | High | Platform audit Phase 1 deep re-run 2026-07-25 | **OPEN** | Define one signed financial reporting model and emit refund/chargeback events. |
| DEF-AUDIT-20 | Testing — browser retries hide state-sensitive failures | A no-retry full MySQL Chromium run passed 77/84 executed journeys but exposed seven tests that depend on retry-mutated state or browser download timing; ordinary CI retries can label these green/flaky. | Medium | Platform audit Phase 1 deep re-run 2026-07-25 | **OPEN** | Provider attestation and one-worker MySQL isolation are fixed; make the seven journeys first-attempt deterministic before enforcing no-retry globally. Changed auth/handoff gates pass 14/14 with no retry. |
| DEF-20 | Scoring consistency — critical-path grading | PCI World grades `critical_path` asks as an **ordered** list (`WorldScore.cs` — positional `Zip` compare) while SimLab grades the identical ask type as an **unordered set** (`SimGrade.cs` — `HashSet` equality). The same learner answer (e.g. `B,A,C` for a path whose canonical order is `A,B,C`) scores correct in the Lab and wrong in a World challenge. 6 published World challenges carry `critical_path` asks. | Medium | Calc-validation Phase 0 (2026-07-25) | **OPEN** (blocked_for_expert_review) | Both policies are defensible (a path's sequence is meaningful; the zero-float activity *set* is also a standard answer form). Harmonising in either direction changes published scoring behaviour, so per the two-derivation rule this needs a product ruling before any change. Documented in `FORMULA_REGISTRY.md` conventions so authors are aware meanwhile. |
| DEF-21 | Content naming — retired PCP-AI branding on public downloads | All eight public study downloads (`docs/downloads/*.md` and their committed `wwwroot/downloads/*.pdf` counterparts — formula sheet, sample questions, blueprint, study guide, glossary, handbook, conduct code) are titled/branded **PCP-AI**, a name `SimContent.RetiredNames` itself classifies as retired (canonical names: PCL-AI/PFL-AI/PML-AI). The Downloads Centre (`PublicDocsSeed.cs`) lists them under neutral titles, so the mismatch surfaces only inside the downloaded files. | Medium | Calc-validation Phase 0 (2026-07-25) | **OPEN** | Requires a coordinated pass: rewrite the markdown corpus, regenerate the PDFs, and decide whether "PCP-AI Body of Knowledge, First Edition" citations are historically-accurate edition branding or debt. Not silently patched here per the do-not-silently-alter-published-content standard. New `QuestionBankTests` meanwhile blocks retired names from the *seeded MCQ banks*. |

| DEF-22 | Tooling — MySQL schema generator dropped a table from its type map | `tools/sqlite_to_mysql.py` parsed CREATE TABLE bodies with a regex that stops at the first `"\n)"`. A one-line table (`site_settings`) closes on the same line as its columns, so the non-greedy match ran past it and swallowed the following block — `code_redemptions` never entered the column-type map, and a regenerated `schema.mysql.sql` emitted `ix_redemptions_email ON code_redemptions(email)` without the `(191)` prefix a TEXT column needs, which Oracle MySQL rejects (BLOB/TEXT index requires a key length). The committed schema was correct only because it predated the drift; any regeneration would have broken the MySQL DDL. | High | Audit-remediation cycle (2026-07-25) — determinism re-check of the generated schema | **FIXED** | `table_bodies()` now scans to the *balanced* closing paren (string-literal and line-comment aware) instead of the fragile `\n)` regex. Regenerated output is byte-identical to the committed `schema.mysql.sql` (SHA-256 `993f2833…`), `--check` passes, and `migration_integrity_test.py` passes 23/23 against Oracle MySQL 8.0.46 including column parity across all 241 shared tables. |
| DEF-23 | Test tooling — S3 storage suite never actually ran (env + stale contract) | `tests/storage_s3_test.py` booted the backend without `ASPNETCORE_ENVIRONMENT`, which ASP.NET defaults to **Production** — the boot config validator refused the throwaway SQLite DB, so the suite's server never came up and the moto-based S3 assertions were unreachable (CI's step is `continue-on-error`, so nobody saw it). Its final assertion also still expected the pre-EXT-P1-09 contract ("no bucket → warn and fall back to local"), which the product deliberately replaced with fail-closed behaviour. | Medium | Audit-remediation cycle (2026-07-25) — RES-020 follow-up, first local moto run | **FIXED** | Both boots now set `ASPNETCORE_ENVIRONMENT=Development`; the fallback assertion now pins the *current* fail-closed contract (boot calls out the missing `S3_BUCKET`; no silent local fallback). Suite passes 9/9 against a live moto S3 server: private-ACL upload, `s3:` reference, no local bytes, authed streaming fetch, retention purge, and the fail-closed no-bucket boot. |
| DEF-24 (RES-026) | Finance — waiver grants lacked a durable request identity | All three admin waiver paths (`/api/admin/students/{id}/waive` full + partial, `/api/admin/exam-fee-waiver` exam/retake/reschedule) were check-then-write: a retried request (network blip, double-click, worker replay) could settle a second $0 payment + membership, mint a second single-use discount code (doubled discount capacity), or grant a second $0 exam seat with a duplicate ledger row. | High | Platform audit register RES-026 (2026-07-25) | **FIXED** | Client-supplied `idempotency_key` on every waiver grant: an existing `fee_waivers` row with the key replays the original outcome (`replay:true` + the original payment/code), a `ux_fee_waivers_idem` unique index is the race backstop (the loser replays the winner), key reuse across students is refused (409 `idempotency_key_conflict`), and keyless requests keep their historical behaviour. Admin UI mints one key per grant intent and rotates it only after confirmed success. Regression: `backend/tests/waiver_idempotency_test.py` (18/18, wired into CI). |

| DEF-25 | Tooling/CI — the MySQL schema gate was made green against incorrect generated output | PR #169 fixed a red `backend-mysql` "Generated MySQL schema is current" gate by regenerating `schema.mysql.sql`, diagnosing the `ix_redemptions_email` `email(191)` prefix as a stale leftover from a bounded column. The column was never bounded — `code_redemptions.email` is still `TEXT` — and the regeneration ran through the DEF-22 generator bug, which silently drops the prefix. The gate then passed because the committed file matched the *incorrect* generator output, which is what masked the generator bug. | Low (no runtime impact — see the correction below) | Audit-remediation cycle (2026-07-25) — merging main into the audit branch | **FIXED** | **Impact correction (measured, not assumed).** An earlier revision of this entry claimed the index was therefore absent on MySQL and that `code_redemptions.email` lookups degraded to full scans. **That is false**, and the claim was retracted after a direct experiment: `Db.Exec` has an explicit `BlobKeyWithoutLength` (1170) handler that retries the statement through `PrefixIndexedColumns`. Booting the backend against a fresh Oracle MySQL 8.0.46 database with the *unprefixed* schema still produces `ix_redemptions_email` with `sub_part = 191` — identical to the prefixed schema. The runtime's defence-in-depth absorbed the defect completely, so there was no production impact at any point. What remains real is narrower and still worth fixing: the generator silently dropped a table from its type map (DEF-22), emitting a generated artifact that does not match what the schema requires, and the CI gate was then re-pinned to that artifact — removing the signal that would have surfaced DEF-22. Generator fixed and `schema.mysql.sql` regenerated (prefix restored); `tools/sqlite_to_mysql.py --check` passes and `migration_integrity_test.py` passes 23/23 on Oracle MySQL 8.0.46. Lesson recorded: a red "schema is current" gate means regenerate *and inspect the diff* — never assume the committed file is the stale side. |
| DEF-26 | Finance — a waiver idempotency key could bridge students | `FeeWaiverLedger.Find` (PR #164) resolved a replay by `idempotency_key` alone, with no check that the key belonged to the subject being waived. Reusing a key for a different student (client that reuses one key, copy-paste, script bug) returned `ok:true, replayed:true` carrying the **first** student's `waiver_id`, `payment_id` and discount code, while granting the second student nothing. Fail-safe against over-granting, but it silently no-ops a legitimate grant — the operator sees success — and discloses an unrelated student's financial record in the response. | Medium | Audit-remediation cycle (2026-07-25) — end-to-end waiver suite (case D1) run against the merged implementation | **FIXED** | New `FeeWaiverLedger.FindForSubject(db, key, userId)` returns a subject-mismatch signal; every pre-check and post-race replay path in `Endpoints/AdminOps.cs` (full + partial) and `Endpoints/ExamExceptions.cs` refuses a cross-subject key with **409 `idempotency_key_conflict`**. Main's transaction-first claim design is otherwise unchanged. Regression: `backend/tests/waiver_idempotency_test.py` D1/D2 (19/19 overall), wired into CI. |

## Notes

- The Phase-0/PR-#73 findings ledger (verify-suspended-credential, membership time-based expiry,
  blog on-demand sweep hook, partner-commission accrual) is **empty** — every one of those was fixed
  and is covered by tests; see `TEST_COVERAGE_MATRIX.md`.
- This register is updated as new increments surface findings. It is the single place a reviewer can
  see what the test programme has learned about the product's real behaviour, separate from the
  coverage matrix (which tracks test presence).
